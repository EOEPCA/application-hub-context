# Hands-on

- Config generator:
Open the example [notebook](./config-generator.ipynb) to generate a `config.yaml` with your desired profile configuration
- Add `eoepca/application-hub` from helm repositories.
- Deploy the ApplicationHub on a remote cluster using skaffold.